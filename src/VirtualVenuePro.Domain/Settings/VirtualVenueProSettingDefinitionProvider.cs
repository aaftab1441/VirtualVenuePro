using Volo.Abp.Settings;

namespace VirtualVenuePro.Settings;

public class VirtualVenueProSettingDefinitionProvider : SettingDefinitionProvider
{
    public override void Define(ISettingDefinitionContext context)
    {
        //Define your own settings here. Example:
        //context.Add(new SettingDefinition(VirtualVenueProSettings.MySetting1));
    }
}
